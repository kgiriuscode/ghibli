export type Film = {
  id: string
  title: string
  description: string
  release_date: string
  people: string[]
}

export type Person = {
  name: string
  age: string
  gender: string
  eye_color: string
}
